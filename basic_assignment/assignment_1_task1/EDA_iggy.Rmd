---
title: "R Notebook"
output: html_notebook
---

```{r}
# load packages
library(tidyverse)
library(rstudioapi)
library(lubridate)
library(arules)

# set working directory
setwd(dirname(getActiveDocumentContext()$path))
# load the data
data <- read_delim("data/final/data_clean.csv", delim = ",")
```

```{r}
# extracting extra information

# extract hour from the bed time answer
data <- data %>% 
  mutate(bed_time_parsed = hms(bed_time))

         bed_hour = hour(bed_time),
         bed_minutes = minute(bed_time))
```


```{r}
## creating extra variables 

extra_var <- data %>% 
  mutate(exact_time = case_when(
    
  ))
```


